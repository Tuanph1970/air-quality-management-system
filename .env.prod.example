# =============================================================================
# Air Quality Management System - Production Environment Variables
# =============================================================================
# Copy this file to .env.prod and fill in your production values.
# NEVER commit .env.prod to version control!
# =============================================================================

# ---------------------------------------------------------------------------
# Service Configuration
# ---------------------------------------------------------------------------
LOG_LEVEL=INFO

# ---------------------------------------------------------------------------
# Database Credentials (Use strong passwords in production!)
# ---------------------------------------------------------------------------
DB_USER=aqms_admin
DB_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD_HERE

# Database Ports (internal only in production - not exposed)
FACTORY_DB_PORT=5432
SENSOR_DB_PORT=5433
ALERT_DB_PORT=5434
USER_DB_PORT=5435
REMOTE_SENSING_DB_PORT=5436

# ---------------------------------------------------------------------------
# RabbitMQ Configuration
# ---------------------------------------------------------------------------
RABBITMQ_USER=aqms_rabbitmq
RABBITMQ_PASS=CHANGE_ME_USE_STRONG_PASSWORD_HERE
RABBITMQ_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672

# ---------------------------------------------------------------------------
# Redis Configuration
# ---------------------------------------------------------------------------
REDIS_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD_HERE
REDIS_PORT=6379

# ---------------------------------------------------------------------------
# JWT Authentication (Generate a strong random secret)
# ---------------------------------------------------------------------------
# Generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_USE_A_LONG_RANDOM_STRING_HERE_MIN_32_CHARS

# ---------------------------------------------------------------------------
# External API Keys - Satellite Data
# ---------------------------------------------------------------------------
# Copernicus Atmosphere Monitoring Service
# Register at: https://ads.atmosphere.copernicus.eu/
COPERNICUS_ADS_API_KEY=your-copernicus-ads-api-key

# NASA Earthdata
# Register at: https://urs.earthdata.nasa.gov/
NASA_EARTHDATA_TOKEN=your-nasa-earthdata-token

# Sentinel Hub
# Register at: https://apps.sentinel-hub.com/oauth-client/
SENTINEL_HUB_CLIENT_ID=your-sentinel-hub-client-id
SENTINEL_HUB_CLIENT_SECRET=your-sentinel-hub-secret

# ---------------------------------------------------------------------------
# Google Maps API (for AQI heatmap visualization)
# ---------------------------------------------------------------------------
# Create at: https://console.cloud.google.com/apis/credentials
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# ---------------------------------------------------------------------------
# City Bounding Box (Default: Ho Chi Minh City, Vietnam)
# Adjust to your monitoring area
# ---------------------------------------------------------------------------
CITY_BBOX_NORTH=11.2
CITY_BBOX_SOUTH=10.3
CITY_BBOX_EAST=107.1
CITY_BBOX_WEST=106.3

# ---------------------------------------------------------------------------
# Satellite Data Fetch Schedule (Cron expressions)
# ---------------------------------------------------------------------------
# CAMS forecast - every 6 hours
CAMS_FETCH_ENABLED=true
CAMS_FETCH_CRON=0 */6 * * *

# MODIS AOD - daily at 02:00 UTC
MODIS_FETCH_ENABLED=true
MODIS_FETCH_CRON=0 2 * * *

# TROPOMI trace gases - daily at 03:00 UTC
TROPOMI_FETCH_ENABLED=true
TROPOMI_FETCH_CRON=0 3 * * *

# ---------------------------------------------------------------------------
# Service Ports (Internal - not exposed in production except frontend)
# ---------------------------------------------------------------------------
API_GATEWAY_PORT=8000
FACTORY_SERVICE_PORT=8001
SENSOR_SERVICE_PORT=8002
ALERT_SERVICE_PORT=8003
AIR_QUALITY_SERVICE_PORT=8004
USER_SERVICE_PORT=8005
REMOTE_SENSING_SERVICE_PORT=8006

# Frontend Port (public facing)
FRONTEND_PORT=80

# ---------------------------------------------------------------------------
# Data Fusion Settings
# ---------------------------------------------------------------------------
FUSION_INTERVAL_MINUTES=60
CALIBRATION_RETRAIN_DAYS=7
CROSS_VALIDATION_TOLERANCE=0.3

# =============================================================================
# SECURITY CHECKLIST FOR PRODUCTION
# =============================================================================
# [ ] Change all CHANGE_ME_* passwords
# [ ] Generate strong JWT secret (min 32 random characters)
# [ ] Add your real API keys for satellite data providers
# [ ] Configure SSL/TLS certificates in nginx/ssl/
# [ ] Update CITY_BBOX_* to your monitoring area
# [ ] Review and adjust resource limits in docker-compose.prod.yml
# [ ] Set up backup strategy for database volumes
# [ ] Configure log aggregation (ELK, Splunk, etc.)
# [ ] Set up monitoring and alerting (Prometheus, Grafana)
# [ ] Review firewall rules - only ports 80/443 should be public
# =============================================================================
